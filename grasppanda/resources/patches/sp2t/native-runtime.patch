--- a/sp2t_native/models/sparse_proxy_point_transformer/proxy_fuser.py
+++ b/sp2t_native/models/sparse_proxy_point_transformer/proxy_fuser.py
@@ -116,7 +116,7 @@
                     rel_pos = positions.unsqueeze(0) - positions.unsqueeze(1)  # N x N x D
                     rel_pos = rearrange(rel_pos, "n1 n2 d -> (n1 n2) d")
                     rpe = self.rpe(rel_pos)
-                    rpe = rearrange(rpe, "(n1 n2) h -> h n1 n2", n1=batch_num_px[0], n2=batch_num_px[0])
+                    rpe = rearrange(rpe, "(n1 n2) h -> h n1 n2", n1=batch_num_px[0], n2=batch_num_px[0]) * math.sqrt(self.d_embed)
             elif "proxy_rpe" in info:
                 rpe = info["proxy_rpe"]
             else:
@@ -143,7 +143,7 @@
             else:
                 q = k = v = token
         else:
-            pad_mask = None
+            pad_mask = batch_px_mask[0] if has_mask else None
             if self.with_pe:
                 q = k = features_with_pe
                 v = features
@@ -230,7 +230,7 @@
         # squeeze and excitation
         if B == 1:
             s = self.do_pool(x)
-            s = self.dense(x)
+            s = self.dense(s)
             e = self.activation(x * s)
         else:
             start = 0
--- a/sp2t_native/models/sparse_proxy_point_transformer/sparse_proxy_ptv3.py
+++ b/sp2t_native/models/sparse_proxy_point_transformer/sparse_proxy_ptv3.py
@@ -111,7 +111,7 @@
     @torch.no_grad()
     def get_rel_pos(self, point, order):
         K = self.patch_size
-        rel_pos_key = f"rel_pos_{self.order_index}"
+        rel_pos_key = f"rel_pos_{self.order_index}_{self.patch_size}"
         if rel_pos_key not in point.keys():
             grid_coord = point.grid_coord[order]
             grid_coord = grid_coord.reshape(-1, K, 3)
@@ -120,9 +120,9 @@
 
     @torch.no_grad()
     def get_padding_and_inverse(self, point):
-        pad_key = "pad"
-        unpad_key = "unpad"
-        cu_seqlens_key = "cu_seqlens_key"
+        pad_key = f"pad_{self.patch_size}"
+        unpad_key = f"unpad_{self.patch_size}"
+        cu_seqlens_key = f"cu_seqlens_key_{self.patch_size}"
         if (
             pad_key not in point.keys()
             or unpad_key not in point.keys()
@@ -403,7 +403,7 @@
                     out_channels=channels,
                     act_layer=act_layer,
                     drop=proj_drop,
-                    enable_checkpoint=True,
+                    enable_checkpoint=enable_checkpoint,
                 )
             )
             self.drop_path = PointSequential(
@@ -793,6 +793,7 @@
                         in_channels=enc_channels[s - 1],
                         out_channels=enc_channels[s],
                         stride=stride[s - 1],
+                        shuffle_orders=shuffle_orders,
                         norm_layer=bn_layer,
                         act_layer=act_layer,
                     ),
--- a/sp2t_native/models/sparse_proxy_point_transformer/sparse_attention.py
+++ b/sp2t_native/models/sparse_proxy_point_transformer/sparse_attention.py
@@ -1,3 +1,23 @@
+from contextlib import contextmanager, nullcontext
+
+
+@contextmanager
+def _checkpoint_buffers(module):
+    buffers = []
+    try:
+        for child in module.modules():
+            if isinstance(child, nn.modules.batchnorm._BatchNorm):
+                for name in ('running_mean', 'running_var', 'num_batches_tracked'):
+                    value = getattr(child, name)
+                    if value is not None:
+                        buffers.append((child, name, value))
+                        setattr(child, name, value.clone())
+        yield
+    finally:
+        for child, name, value in buffers:
+            setattr(child, name, value)
+
+
 import math
 
 import numpy as np
@@ -95,7 +115,7 @@
         use_native = self.use_warp or self.use_taichi
         if self.training or not use_native:
             if self.enable_checkpoint:
-                out, weights = checkpoint(self.forward_torch, q, k, v, associations, bias, use_reentrant=False)
+                out, weights = checkpoint(self.forward_torch, q, k, v, associations, bias, use_reentrant=False, context_fn=lambda: (nullcontext(), _checkpoint_buffers(self)))
             else:
                 out, weights = self.forward_torch(q, k, v, associations, bias)
         else:
@@ -244,7 +264,7 @@
 
             # get q by pooling
             if self.reduce_query != "none":
-                q.index_reduce_(0, dst_ids, k_expand, self.reduce_query, include_self=True)
+                q = q.index_reduce(0, dst_ids, k_expand, self.reduce_query, include_self=True)
             q_expand = q[dst_ids]
             
             if self.include_self:
@@ -280,7 +300,7 @@
 
             # subtract max
             if self.include_self:
-                max_sim = self_sim
+                max_sim = self_sim.clone()
             else:
                 max_sim = torch.full([Q, H], -torch.inf, device=device, dtype=torch.float32)
             max_sim.index_reduce_(0, dst_ids, sim, "amax", include_self=True)
@@ -435,7 +455,7 @@
         use_native = self.use_warp or self.use_taichi
         if self.training or not use_native:
             if self.enable_checkpoint:
-                out, weights = checkpoint(self.forward_torch, q, k, v, associations, bias, use_reentrant=False)
+                out, weights = checkpoint(self.forward_torch, q, k, v, associations, bias, use_reentrant=False, context_fn=lambda: (nullcontext(), _checkpoint_buffers(self)))
             else:
                 out, weights = self.forward_torch(q, k, v, associations, bias)
         else:
--- a/sp2t_native/models/sparse_proxy_point_transformer/proxy_initializer.py
+++ b/sp2t_native/models/sparse_proxy_point_transformer/proxy_initializer.py
@@ -1,3 +1,4 @@
+from contextvars import ContextVar
 import numpy as np
 import math
 
@@ -572,6 +573,12 @@
 
 class ProxyInitializer(nn.Module):
     GLB_STATES = {}
+    GLB_CONTEXT = ContextVar('sp2t_proxy_states', default=None)
+
+    @staticmethod
+    def state():
+        scoped = ProxyInitializer.GLB_CONTEXT.get()
+        return ProxyInitializer.GLB_STATES if scoped is None else scoped
 
     def __init__(
         self,
@@ -613,7 +620,7 @@
         # rel bias (if needed)
         self.need_rel_bias = rel_bias_cfg is not None
         self.need_rel_bias = self.need_rel_bias and (not reuse_rel_bias or self.is_first_block)
-        if self.need_rel_bias:
+        if self.need_rel_bias and not (self.lvl_wise_reuse and self.is_first_block):
             self.rel_bias = TableEmbed3D(**rel_bias_cfg)
         # rpe (if needed)
         self.need_rpe = rpe_cfg is not None
@@ -630,18 +637,18 @@
             )
 
     def clr_glb_state(self):
-        ProxyInitializer.GLB_STATES = {}
+        ProxyInitializer.state().clear()
 
     def store_glb_state(self, key, val):
         key = f"rank{comm.get_rank()}/{key}"
-        ProxyInitializer.GLB_STATES[key] = val
+        ProxyInitializer.state()[key] = val
 
     def get_glb_state(self, key, default=None):
         key = f"rank{comm.get_rank()}/{key}"
-        if default is None and key not in ProxyInitializer.GLB_STATES:
-            existing_keys = list(ProxyInitializer.GLB_STATES.keys())
+        if default is None and key not in ProxyInitializer.state():
+            existing_keys = list(ProxyInitializer.state().keys())
             raise ValueError(f"given key [{key}] is not in GLB_STATES with keys [{existing_keys}]")
-        return ProxyInitializer.GLB_STATES.get(key, default)
+        return ProxyInitializer.state().get(key, default)
 
     def gen_px_rpe(self, proxy_positions, batch_num_px):
         maxlen = max(batch_num_px)
@@ -689,6 +696,7 @@
                     batch_num_px.append(num_px)
                     batch_px_pos.append(px_pos)
                     batch_init_info.append(init_info)
+                    pt_start = pt_end
                 proxy_positions = torch.cat(batch_px_pos, dim=0)
             else:
                 info = self.get_glb_state("proxy_info")
