--- a/Swin3D/modules/swin3d_layers.py
+++ b/Swin3D/modules/swin3d_layers.py
@@ -217,11 +217,20 @@
         in_map, out_map = map_pair
         broad_min_dist_sp = self.unpool(min_dist_sp)
         mask = (broad_min_dist_sp.F == dist_sp.F).squeeze(1)
-        in_map = in_map[mask].long()
-        out_map = out_map[mask].long()
-        downsample_map = torch.zeros(N, dtype=torch.long, device=device) - 1
-        downsample_map[out_map] = in_map
-        assert (downsample_map >= 0).all()
+        selected = mask[in_map.long()]
+        in_map = in_map[selected].long()
+        out_map = out_map[selected].long()
+        # Equal-distance representatives must not race through duplicate writes.
+        # Select the lexicographically first source voxel among native minima.
+        order = torch.arange(len(coords_sp.C), device=device)
+        for axis in (3, 2, 1, 0):
+            order = order[torch.argsort(coords_sp.C[order, axis], stable=True)]
+        rank = torch.empty_like(order)
+        rank[order] = torch.arange(len(order), device=device)
+        chosen = torch.full((N,), len(order), dtype=torch.long, device=device)
+        chosen.scatter_reduce_(0, out_map, rank[in_map], reduce="amin", include_self=True)
+        assert (chosen < len(order)).all()
+        downsample_map = order[chosen]
         assert (dist_sp.F[downsample_map] == min_dist_sp.F).all()
         new_coords = coords_sp.F[downsample_map]
         new_coords_sp = assign_feats(sp, new_coords)
@@ -236,7 +245,7 @@
     bs = batch.max() + 1
     for i in range(bs):
         offset.append(torch.sum(batch == i))
-    offset = torch.cuda.IntTensor(offset)
+    offset = torch.stack(offset).to(dtype=torch.int32)
     offset = offset.cumsum(dim=0).int()
     return offset
 
@@ -508,7 +517,7 @@
         query, key, value = qkv[0], qkv[1], qkv[2]  # [N, num_heads, C//num_heads]
         query = query * self.scale
 
-        table_offsets = torch.IntTensor(self.table_offsets).cuda()
+        table_offsets = torch.tensor(self.table_offsets, dtype=torch.int32, device=feats.device)
         query_table, key_table, value_table = [], [], []
         n_cRSE = []
         if "XYZ" in self.cRSE:
--- a/Swin3D/src/attn/attn_utils.cuh
+++ b/Swin3D/src/attn/attn_utils.cuh
@@ -446,7 +446,7 @@
             auto table_size = table_sizes_ptr[n];
             auto window_size = table_size / hc_stride;
             rel_pos += window_size / 2;
-            rel_pos = fminf(fmaxf(0, rel_pos), window_size - 1e-10);
+            rel_pos = fminf(fmaxf(0, rel_pos), float(window_size - 1));
             qk_coords[n] = ((int)rel_pos * hc_stride + table_offset + hc_offset) / num_packed;
             table_offset += table_size;
         }
--- a/Swin3D/src/attn/self_attn_apply_coff_indir_backward.cu
+++ b/Swin3D/src/attn/self_attn_apply_coff_indir_backward.cu
@@ -1,3 +1,5 @@
+#include <c10/cuda/CUDAGuard.h>
+#include <c10/cuda/CUDAStream.h>
 /*
 Copyright (c) Microsoft Corporation.
 Licensed under the MIT License.
@@ -195,6 +197,7 @@
     torch::Tensor n_coords,
     int64_t pos_bias_method)
 {
+    const c10::cuda::CUDAGuard device_guard(updated_value_grads.device());
     CHECK_INPUT(updated_value_grads);
     CHECK_INPUT(value_feats);
     CHECK_INPUT(coff_norm_feats);
@@ -222,7 +225,7 @@
     const dim3 blocks((total_size + NUM_THREADS - 1) / NUM_THREADS);
 
     AT_DISPATCH_FLOATING_TYPES_AND_HALF(value_feats.type(), "self_attn_apply_coff_cuda_backward_kernel", ([&] {
-        self_attn_apply_coff_cuda_backward_indir_kernel<<<blocks, NUM_THREADS>>>(
+        self_attn_apply_coff_cuda_backward_indir_kernel<<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
             updated_value_grads.data_ptr<scalar_t>(),
             value_feats.data_ptr<scalar_t>(),
             coff_norm_feats.data_ptr<scalar_t>(),
--- a/Swin3D/src/attn/self_attn_cal_coff_indir_forward.cu
+++ b/Swin3D/src/attn/self_attn_cal_coff_indir_forward.cu
@@ -1,3 +1,5 @@
+#include <c10/cuda/CUDAGuard.h>
+#include <c10/cuda/CUDAStream.h>
 /*
 Copyright (c) Microsoft Corporation.
 Licensed under the MIT License.
@@ -156,6 +158,7 @@
     torch::Tensor n_coords,
     int64_t pos_bias_method) 
 {
+    const c10::cuda::CUDAGuard device_guard(query_feats.device());
     CHECK_INPUT(query_feats);
     CHECK_INPUT(key_feats);
     CHECK_INPUT(query_table);
@@ -183,7 +186,7 @@
     const dim3 blocks((total_size + NUM_THREADS - 1) / NUM_THREADS);
 
     AT_DISPATCH_FLOATING_TYPES_AND_HALF(coffs.type(), "coff_self_attn_kernel_forward", ([&] {
-        self_attn_cal_coff_cuda_forward_indir_kernel<<<blocks, NUM_THREADS>>>(
+        self_attn_cal_coff_cuda_forward_indir_kernel<<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
             query_feats.data_ptr<scalar_t>(),
             key_feats.data_ptr<scalar_t>(),
             query_table.data_ptr<scalar_t>(),
--- a/Swin3D/src/attn/self_attn_apply_coff_indir_forward.cu
+++ b/Swin3D/src/attn/self_attn_apply_coff_indir_forward.cu
@@ -1,3 +1,5 @@
+#include <c10/cuda/CUDAGuard.h>
+#include <c10/cuda/CUDAStream.h>
 /*
 Copyright (c) Microsoft Corporation.
 Licensed under the MIT License.
@@ -138,6 +140,7 @@
     torch::Tensor n_coords,
     int64_t pos_bias_method)
 {
+    const c10::cuda::CUDAGuard device_guard(value_feats.device());
     CHECK_INPUT(value_feats);
     CHECK_INPUT(coff_norm_feats);
     CHECK_INPUT(value_table);
@@ -162,7 +165,7 @@
     const dim3 blocks((total_size + NUM_THREADS - 1) / NUM_THREADS);
 
     AT_DISPATCH_FLOATING_TYPES_AND_HALF(value_feats.type(), "self_attn_apply_coff_cuda_forward_kernel", ([&] {
-        self_attn_apply_coff_cuda_forward_indir_kernel<<<blocks, NUM_THREADS>>>(
+        self_attn_apply_coff_cuda_forward_indir_kernel<<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
             value_feats.data_ptr<scalar_t>(),
             coff_norm_feats.data_ptr<scalar_t>(),
             value_table.data_ptr<scalar_t>(),
--- a/Swin3D/src/attn/self_attn_aio_fwd.cu
+++ b/Swin3D/src/attn/self_attn_aio_fwd.cu
@@ -1,3 +1,5 @@
+#include <c10/cuda/CUDAGuard.h>
+#include <c10/cuda/CUDAStream.h>
 /*
 Copyright (c) Microsoft Corporation.
 Licensed under the MIT License.
@@ -164,7 +166,7 @@
 
     __syncthreads();
     auto p_idx = threadIdx.x / (index_table.num_channels / index_table.packed_stride);
-    __shared__ scalar_t sum_coffs[32];
+    __shared__ scalar_t sum_coffs[NUM_THREADS];
 
     if (index_table.g_idx < total_size * index_table.packed_stride)
     {
@@ -261,6 +263,7 @@
     int64_t pos_mode
 )
 {
+    const c10::cuda::CUDAGuard device_guard(qkv_feats[0].device());
     auto query_feats = qkv_feats[0];
     auto key_feats = qkv_feats[1];
     auto value_feats = qkv_feats[2];
@@ -312,7 +315,7 @@
             switch(num_dim)
             {
             case 3:
-                cal_coff_raw_max_kernel_dir<scalar_t, accscalar_t, 3><<<blocks, NUM_THREADS>>>(
+                cal_coff_raw_max_kernel_dir<scalar_t, accscalar_t, 3><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     query_table.data_ptr<scalar_t>(),
@@ -328,7 +331,7 @@
                 );
                 break;
             case 6:
-                cal_coff_raw_max_kernel_dir<scalar_t, accscalar_t, 6><<<blocks, NUM_THREADS>>>(
+                cal_coff_raw_max_kernel_dir<scalar_t, accscalar_t, 6><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     query_table.data_ptr<scalar_t>(),
@@ -344,7 +347,7 @@
                 );
                 break;
             case 9:
-                cal_coff_raw_max_kernel_dir<scalar_t, accscalar_t, 9><<<blocks, NUM_THREADS>>>(
+                cal_coff_raw_max_kernel_dir<scalar_t, accscalar_t, 9><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     query_table.data_ptr<scalar_t>(),
@@ -387,7 +390,7 @@
             switch(num_dim)
             {
             case 3:
-                cal_coff_raw_max_kernel_indir<scalar_t, accscalar_t, 3><<<blocks, NUM_THREADS>>>( 
+                cal_coff_raw_max_kernel_indir<scalar_t, accscalar_t, 3><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>( 
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     query_table.data_ptr<scalar_t>(),
@@ -405,7 +408,7 @@
                 );
                 break;
             case 6:
-                cal_coff_raw_max_kernel_indir<scalar_t, accscalar_t, 6><<<blocks, NUM_THREADS>>>( 
+                cal_coff_raw_max_kernel_indir<scalar_t, accscalar_t, 6><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>( 
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     query_table.data_ptr<scalar_t>(),
@@ -423,7 +426,7 @@
                 );
                 break;
             case 9:
-                cal_coff_raw_max_kernel_indir<scalar_t, accscalar_t, 9><<<blocks, NUM_THREADS>>>( 
+                cal_coff_raw_max_kernel_indir<scalar_t, accscalar_t, 9><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>( 
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     query_table.data_ptr<scalar_t>(),
@@ -463,6 +466,7 @@
     int64_t indice_mode,
     int64_t pos_mode) 
 {
+    const c10::cuda::CUDAGuard device_guard(qkv_feats[0].device());
     auto query_feats = qkv_feats[0];
     auto key_feats = qkv_feats[1];
     auto value_feats = qkv_feats[2];
@@ -518,7 +522,7 @@
             switch(num_dim)
             {
             case 3:
-                self_attn_cuda_forward_kernel_dir<scalar_t, accscalar_t, 3><<<blocks, NUM_THREADS>>>(
+                self_attn_cuda_forward_kernel_dir<scalar_t, accscalar_t, 3><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     value_feats.data_ptr<scalar_t>(),
@@ -538,7 +542,7 @@
                 );
                 break;
             case 6:
-                self_attn_cuda_forward_kernel_dir<scalar_t, accscalar_t, 6><<<blocks, NUM_THREADS>>>(
+                self_attn_cuda_forward_kernel_dir<scalar_t, accscalar_t, 6><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     value_feats.data_ptr<scalar_t>(),
@@ -558,7 +562,7 @@
                 );
                 break;
             case 9:
-                self_attn_cuda_forward_kernel_dir<scalar_t, accscalar_t, 9><<<blocks, NUM_THREADS>>>(
+                self_attn_cuda_forward_kernel_dir<scalar_t, accscalar_t, 9><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     value_feats.data_ptr<scalar_t>(),
@@ -608,7 +612,7 @@
             switch(num_dim)
             {
             case 3:
-                self_attn_cuda_forward_kernel_indir<scalar_t, accscalar_t, 3><<<blocks, NUM_THREADS>>>(
+                self_attn_cuda_forward_kernel_indir<scalar_t, accscalar_t, 3><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     value_feats.data_ptr<scalar_t>(),
@@ -630,7 +634,7 @@
                 );
                 break;
             case 6:
-                self_attn_cuda_forward_kernel_indir<scalar_t, accscalar_t, 6><<<blocks, NUM_THREADS>>>(
+                self_attn_cuda_forward_kernel_indir<scalar_t, accscalar_t, 6><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     value_feats.data_ptr<scalar_t>(),
@@ -652,7 +656,7 @@
                 );
                 break;
             case 9:
-                self_attn_cuda_forward_kernel_indir<scalar_t, accscalar_t, 9><<<blocks, NUM_THREADS>>>(
+                self_attn_cuda_forward_kernel_indir<scalar_t, accscalar_t, 9><<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     query_feats.data_ptr<scalar_t>(),
                     key_feats.data_ptr<scalar_t>(),
                     value_feats.data_ptr<scalar_t>(),
--- a/Swin3D/src/attn/self_attn_cal_coff_indir_backward.cu
+++ b/Swin3D/src/attn/self_attn_cal_coff_indir_backward.cu
@@ -1,3 +1,5 @@
+#include <c10/cuda/CUDAGuard.h>
+#include <c10/cuda/CUDAStream.h>
 /*
 Copyright (c) Microsoft Corporation.
 Licensed under the MIT License.
@@ -196,6 +198,7 @@
     torch::Tensor n_coords,
     int64_t pos_bias_method)
 {
+    const c10::cuda::CUDAGuard device_guard(coff_grads.device());
     CHECK_INPUT(coff_grads);
     CHECK_INPUT(query_feats);
     CHECK_INPUT(key_feats);
@@ -226,7 +229,7 @@
     const dim3 blocks((total_size + NUM_THREADS - 1) / NUM_THREADS);
 
     AT_DISPATCH_FLOATING_TYPES_AND_HALF(coff_grads.type(), "coff_self_attn_kernel_backward", ([&] {
-        self_attn_cal_coff_cuda_backward_indir_kernel<<<blocks, NUM_THREADS>>>(
+        self_attn_cal_coff_cuda_backward_indir_kernel<<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
             coff_grads.data_ptr<scalar_t>(),
             query_feats.data_ptr<scalar_t>(),
             key_feats.data_ptr<scalar_t>(),
--- a/Swin3D/src/attn/self_attn_aio_bwd.cu
+++ b/Swin3D/src/attn/self_attn_aio_bwd.cu
@@ -1,3 +1,5 @@
+#include <c10/cuda/CUDAGuard.h>
+#include <c10/cuda/CUDAStream.h>
 /*
 Copyright (c) Microsoft Corporation.
 Licensed under the MIT License.
@@ -202,6 +204,7 @@
     torch::Tensor value_table
 )
 {
+    const c10::cuda::CUDAGuard device_guard(normed_out_grads.device());
     auto num_voxel = value_feats.size(0);
     auto num_head = value_feats.size(1);
     auto num_channel = value_feats.size(2);
@@ -216,7 +219,7 @@
     AT_DISPATCH_FLOATING_TYPES_AND_HALF(value_feats.type(), "self_attn_cuda_s1_backward_kernel", ([&] {
         using accscalar_t = acc_type<scalar_t>;
 
-        cal_exp_sum_grad_kernel<scalar_t, accscalar_t><<<blocks_s1, NUM_THREADS>>>(
+        cal_exp_sum_grad_kernel<scalar_t, accscalar_t><<<blocks_s1, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
             normed_out_grads.data_ptr<scalar_t>(),
             unnormed_out_feats.data_ptr<scalar_t>(),
             exp_sum_feats.data_ptr<scalar_t>(),
@@ -247,6 +250,7 @@
     int64_t indice_mode,
     int64_t pos_bias_method)
 {
+    const c10::cuda::CUDAGuard device_guard(normed_out_grads.device());
     CHECK_INPUT(normed_out_grads);
     CHECK_INPUT(unnormed_out_feats);
     CHECK_INPUT(exp_sum_feats);
@@ -296,7 +300,7 @@
             switch(num_dim)
             {
             case 3:
-                self_attn_cuda_backward_kernel_dir<scalar_t, accscalar_t, 3><<<blocks_s2, NUM_THREADS>>>(  
+                self_attn_cuda_backward_kernel_dir<scalar_t, accscalar_t, 3><<<blocks_s2, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(  
                         normed_out_grads.data_ptr<scalar_t>(),
                         exp_sum_grads.data_ptr<scalar_t>(),
                         exp_sum_feats.data_ptr<scalar_t>(),
@@ -323,7 +327,7 @@
                     );
                 break;
             case 6:
-                self_attn_cuda_backward_kernel_dir<scalar_t, accscalar_t, 6><<<blocks_s2, NUM_THREADS>>>(  
+                self_attn_cuda_backward_kernel_dir<scalar_t, accscalar_t, 6><<<blocks_s2, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(  
                         normed_out_grads.data_ptr<scalar_t>(),
                         exp_sum_grads.data_ptr<scalar_t>(),
                         exp_sum_feats.data_ptr<scalar_t>(),
@@ -350,7 +354,7 @@
                     );
                 break;
             case 9:
-                self_attn_cuda_backward_kernel_dir<scalar_t, accscalar_t, 9><<<blocks_s2, NUM_THREADS>>>(  
+                self_attn_cuda_backward_kernel_dir<scalar_t, accscalar_t, 9><<<blocks_s2, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(  
                         normed_out_grads.data_ptr<scalar_t>(),
                         exp_sum_grads.data_ptr<scalar_t>(),
                         exp_sum_feats.data_ptr<scalar_t>(),
@@ -407,7 +411,7 @@
             switch(num_dim)
             {
             case 3:
-                self_attn_cuda_backward_kernel_indir<scalar_t, accscalar_t, 3><<<blocks_s2, NUM_THREADS>>>(
+                self_attn_cuda_backward_kernel_indir<scalar_t, accscalar_t, 3><<<blocks_s2, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     normed_out_grads.data_ptr<scalar_t>(),
                     exp_sum_grads.data_ptr<scalar_t>(),
                     exp_sum_feats.data_ptr<scalar_t>(),
@@ -436,7 +440,7 @@
                 );
                 break;
             case 6:
-                self_attn_cuda_backward_kernel_indir<scalar_t, accscalar_t, 6><<<blocks_s2, NUM_THREADS>>>(
+                self_attn_cuda_backward_kernel_indir<scalar_t, accscalar_t, 6><<<blocks_s2, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     normed_out_grads.data_ptr<scalar_t>(),
                     exp_sum_grads.data_ptr<scalar_t>(),
                     exp_sum_feats.data_ptr<scalar_t>(),
@@ -465,7 +469,7 @@
                 );
                 break;
             case 9:
-                self_attn_cuda_backward_kernel_indir<scalar_t, accscalar_t, 9><<<blocks_s2, NUM_THREADS>>>(
+                self_attn_cuda_backward_kernel_indir<scalar_t, accscalar_t, 9><<<blocks_s2, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>(
                     normed_out_grads.data_ptr<scalar_t>(),
                     exp_sum_grads.data_ptr<scalar_t>(),
                     exp_sum_feats.data_ptr<scalar_t>(),
--- a/Swin3D/src/knn/knn_cuda_kernel.cu
+++ b/Swin3D/src/knn/knn_cuda_kernel.cu
@@ -1,3 +1,5 @@
+#include <c10/cuda/CUDAGuard.h>
+#include <c10/cuda/CUDAStream.h>
 #include "cuda_utils.cuh"
 #include "knn_cuda_kernel.h"
 
@@ -212,6 +214,7 @@
     torch::Tensor query_offset, 
     torch::Tensor ret_idx, 
     torch::Tensor ret_dist){
+    const c10::cuda::CUDAGuard device_guard(src_xyz.device());
 
     CHECK_INPUT(src_xyz);
     CHECK_INPUT(query_xyz);
@@ -225,10 +228,10 @@
     auto batch_size = src_offset.size(0);
     
     assert(batch_size == query_offset.size(0));
-    assert(K<=K_MAX);
+    TORCH_CHECK(K > 0 && K <= K_MAX, "K must be between 1 and 64");
 
     const dim3 blocks((query_N + NUM_THREADS - 1) / NUM_THREADS);
-    knn_cuda_kernel<<<blocks, NUM_THREADS>>>
+    knn_cuda_kernel<<<blocks, NUM_THREADS, 0, c10::cuda::getCurrentCUDAStream()>>>
     (   
         batch_size,
         src_N,
--- a/Swin3D/sparse_dl/knn/knn.py
+++ b/Swin3D/sparse_dl/knn/knn.py
@@ -9,10 +9,24 @@
 class KNN(Function):
     @staticmethod
     def forward(ctx, K, src_xyz, query_xyz, src_offset, query_offset):
-        assert src_xyz.is_contiguous() and query_xyz.is_contiguous()
+        if type(K) is not int or not 1 <= K <= 64:
+            raise ValueError("K must be an integer between 1 and 64")
+        tensors = (src_xyz, query_xyz, src_offset, query_offset)
+        if not src_xyz.is_cuda or any(t.device != src_xyz.device or not t.is_contiguous() for t in tensors):
+            raise ValueError("KNN inputs must be contiguous tensors on one CUDA device")
+        if any(t.dtype != torch.float32 or t.ndim != 2 or t.shape[1] != 3 for t in (src_xyz, query_xyz)):
+            raise ValueError("KNN coordinates must be float32 [N, 3] tensors")
+        if any(t.dtype != torch.int32 or t.ndim != 1 for t in (src_offset, query_offset)) or len(src_offset) != len(query_offset) or not len(src_offset):
+            raise ValueError("KNN offsets must be matching nonempty int32 vectors")
+        counts = torch.diff(src_offset, prepend=src_offset.new_zeros(1))
+        query_counts = torch.diff(query_offset, prepend=query_offset.new_zeros(1))
+        if int(src_offset[-1]) != len(src_xyz) or int(query_offset[-1]) != len(query_xyz) or bool((counts < K).any()) or bool((query_counts < 0).any()):
+            raise ValueError("Each source scene needs at least K points and valid cumulative offsets")
         N = query_xyz.shape[0]
-        ret_idx = torch.cuda.IntTensor(N, K).zero_()
-        ret_dist = torch.cuda.FloatTensor(N, K).zero_() + 1e10
+        ret_idx = torch.zeros((N, K), dtype=torch.int32, device=src_xyz.device)
+        ret_dist = torch.full((N, K), 1e10, dtype=torch.float32, device=src_xyz.device)
+        if N == 0:
+            return ret_idx, ret_dist
         knn_cuda.knn_cuda(
                 K, 
                 src_xyz, 
--- a/Swin3D/sparse_dl/attn/attn_coff.py
+++ b/Swin3D/sparse_dl/attn/attn_coff.py
@@ -191,11 +191,12 @@
     @staticmethod
     def backward(ctx: Any, *grad_outputs: Any) -> Any:
         norm_attn_grads, = grad_outputs
-        sum_coffs, coff_rmax, norm_attn_feats = ctx.saved_tensors[:3]
-        raw_query_feats, raw_key_feats, raw_value_feats = ctx.saved_tensors[3:6]
-        query_table, key_table, value_table = ctx.saved_tensors[6:9]
-        table_offsets = ctx.saved_tensors[9]
-        indices = ctx.saved_tensors[10:]
+        saved = ctx.saved_tensors
+        sum_coffs, coff_rmax, norm_attn_feats = saved[:3]
+        raw_query_feats, raw_key_feats, raw_value_feats = saved[3:6]
+        query_table, key_table, value_table = saved[6:9]
+        table_offsets = saved[9]
+        indices = saved[10:]
 
         pos_emb: PosEmb = getattr(ctx, "pe")
         mode: IndexMode = getattr(ctx, 'mode')
