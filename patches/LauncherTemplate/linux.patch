--- a/LauncherTemplate/__init__.py
+++ b/LauncherTemplate/__init__.py
@@ -44,7 +44,7 @@
 
 def get_gpu_memory(verbose=True) -> dict[tuple[int, int]]:
     command = "nvidia-smi --query-gpu=index,name,memory.used,memory.total --format=csv,nounits,noheader"
-    output = subprocess.check_output(command, stderr=subprocess.STDOUT).decode("utf-8", errors="ignore")
+    output = subprocess.check_output(command.split(), stderr=subprocess.STDOUT).decode("utf-8", errors="ignore")
     lines = output.strip().split("\n")
 
     gpu_memory = {}
--- a/LauncherTemplate/Trainer.py
+++ b/LauncherTemplate/Trainer.py
@@ -5,7 +5,9 @@
     _DatasetWrapper, MetricTracker, MetricSpec
 
 import os
-import win32com.client
+import sys
+if sys.platform == "win32":
+    import win32com.client
 import torch
 from torch import Tensor
 import torch.distributed as dist
--- a/LauncherTemplate/BaseLauncher.py
+++ b/LauncherTemplate/BaseLauncher.py
@@ -8,7 +8,8 @@
 import torch
 import sys
 import io
-import win32com.client
+if sys.platform == "win32":
+    import win32com.client
 from typing import Union
 import platform
 import os
@@ -22,3 +23,8 @@
 from torch.utils.data import DataLoader, Dataset
 
 def create_folder_shortcut(folder_path, shortcut_path, description=""):    
+    if sys.platform != "win32":
+        target = os.path.abspath(folder_path)
+        if not os.path.lexists(shortcut_path):
+            os.symlink(target, shortcut_path, target_is_directory=True)
+        return
